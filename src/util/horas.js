
export function difHorStr(cHorIni, cHorFim) {
    // StrHora das duas
    // Diferença * 24
}

export function HoraToFloat(cHora) {
    // Valida hora
    // Pega primeira parte
    // Soma a com (segunda parte e divide por 60)
    // Truncar as casas decimais???
}